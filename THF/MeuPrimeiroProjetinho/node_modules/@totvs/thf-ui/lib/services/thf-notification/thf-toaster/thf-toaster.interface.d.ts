import { ThfNotification } from './../thf-notification.interface';
import { ThfToasterType } from './thf-toaster-type.enum';
/**
 * @docsPrivate
 *
 * Interface para os dados do serviço do thf-toaster.
 */
export interface ThfToaster extends ThfNotification {
    /** ID do toaster */
    componentRef?: any;
    /** Posição para notificação aparecer na tela. */
    position: number;
    /** Tipo de notificação. */
    type: ThfToasterType;
}
