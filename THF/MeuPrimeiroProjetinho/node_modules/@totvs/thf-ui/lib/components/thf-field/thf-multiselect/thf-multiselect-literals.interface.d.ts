/**
 * @usedBy ThfMultiselectComponent
 *
 * @description
 *
 * Interface para definição das literais usadas no `thf-multiselect`.
 */
export interface ThfMultiselectLiterals {
    /** Texto exibido quando não houver dados encontrados na busca. */
    noData?: string;
    /** Texto do *placeholder* do campo de busca. */
    placeholderSearch?: string;
}
