import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { ThfComboFilter } from './interfaces/thf-combo-filter.interface';
import { ThfComboOption } from './interfaces/thf-combo-option.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Serviço padrão utilizado para filtrar os dados do componente thf-combo.
 */
export declare class ThfComboFilterService implements ThfComboFilter {
    private http;
    private _url;
    fieldLabel: string;
    fieldValue: string;
    readonly url: string;
    constructor(http: HttpClient);
    getFilteredData(param: any, filterParams?: any): Observable<Array<ThfComboOption>>;
    getObjectByValue(value: string | number, filterParams?: any): Observable<ThfComboOption>;
    configProperties(url: string, fieldLabel: string, fieldValue: string): void;
    private parseToArrayComboOption;
    private parseToComboOption;
}
