import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { ThfLookupFilter } from '../interfaces/thf-lookup-filter.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Serviço padrão utilizado para filtrar os dados do componente thf-lookup.
 */
export declare class ThfLookupFilterService implements ThfLookupFilter {
    private httpClient;
    private url;
    constructor(httpClient: HttpClient);
    getFilteredData(filter: any, page: number, pageSize?: number, filterParams?: any): Observable<any>;
    getObjectByValue(value: string, filterParams?: any): Observable<any>;
    setUrl(url: string): void;
    private validateParams;
}
