import { AfterContentChecked, EventEmitter, OnInit } from '@angular/core';
import { AbstractControl, ControlValueAccessor, Validator } from '@angular/forms';
import { ThfMultiselectFilterMode } from './thf-multiselect-filter-mode.enum';
import { ThfMultiselectLiterals } from './thf-multiselect-literals.interface';
import { ThfMultiselectOption } from './thf-multiselect-option.interface';
export declare const thfMultiselectLiteralsDefault: {
    en: ThfMultiselectLiterals;
    es: ThfMultiselectLiterals;
    pt: ThfMultiselectLiterals;
};
/**
 * @description
 *
 * O thf-multiselect é um componente de múltipla seleção.
 * Este componente é recomendado para dar ao usuário a opção de selecionar vários itens em uma lista.
 *
 * Quando a lista possuir poucos itens, deve-se dar preferência para o uso do thf-checkbox-group, por ser mais simples
 * e mais rápido para a seleção do usuário.
 *
 * Este componente também não deve ser utilizado em casos onde a seleção seja única. Nesses casos, deve-se utilizar o
 * thf-select, thf-combo ou thf-radio-group.
 */
export declare abstract class ThfMultiselectBaseComponent implements AfterContentChecked, ControlValueAccessor, OnInit, Validator {
    private _disabled?;
    private _filterMode?;
    private _focus?;
    private _hideSearch?;
    private _literals;
    private _options;
    private _required?;
    private _sort?;
    private lastLengthModel;
    private onModelChange;
    private onModelTouched;
    private readyToValidation;
    selectedOptions: Array<ThfMultiselectOption>;
    visibleOptionsDropdown: Array<ThfMultiselectOption>;
    visibleDisclaimers: any[];
    /** Label no componente. */
    label?: string;
    /** Texto de apoio para o campo. */
    help?: string;
    /**
     * @optional
     *
     * @description
     *
     * Objeto com as literais usadas no `thf-multiselect`.
     *
     * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
     *
     * ```
     *  const customLiterals: ThfMultiselectLiterals = {
     *    noData: 'Nenhum dado encontrado',
     *    placeholderSearch: 'Buscar'
     *  };
     * ```
     *
     * Ou passando apenas as literais que deseja customizar:
     *
     * ```
     *  const customLiterals: ThfMultiselectLiterals = {
     *    noData: 'Sem dados'
     *  };
     * ```
     *
     * E para carregar as literais customizadas, basta apenas passar o objeto para o componente:
     *
     * ```
     * <thf-multiselect
     *   [t-literals]="customLiterals">
     * </thf-thf-multiselect>
     * ```
     *
     *  > O objeto padrão de literais será traduzido de acordo com o idioma do *browser* (pt, en, es).
     */
    literals: ThfMultiselectLiterals;
    /**
     * @optional
     *
     * @description
     *
     * Define se a indicação de campo opcional será exibida.
     *
     * > Não será exibida a indicação se:
     * - O campo conter `t-required`;
     * - Não possuir `t-help` e/ou `t-label`.
     *
     * @default `false`
     */
    optional: boolean;
    /** Mensagem apresentada enquanto o campo estiver vazio. */
    placeholder?: string;
    /**
     * @description
     *
     * Placeholder do campo de pesquisa.
     *
     * > Caso o mesmo não seja informado, o valor padrão será traduzido com base no idioma do navegador (pt, es e en).
     *
     * @default `Buscar`
     */
    placeholderSearch?: string;
    /** Nome do componente. */
    name: string;
    /**
     * @optional
     *
     * @description
     *
     * Indica que o campo será obrigatório. Esta propriedade é desconsiderada quando o campo está desabilitado (t-disabled).
     *
     * @default `false`
     */
    required: boolean;
    /**
     * @optional
     *
     * @description
     *
     * Indica que o campo será desabilitado.
     *
     * @default `false`
     */
    disabled: boolean;
    /**
     * @optional
     *
     * @description
     *
     * Esconde o campo de pesquisa existente dentro do dropdown do thf-multiselect.
     *
     * @default `false`
     */
    hideSearch: boolean;
    /**
     * @description
     *
     * Nesta propriedade deve ser definida uma lista de objetos que implementam a interface ThfMultiselectOption.
     * Esta lista deve conter os valores e os labels que serão apresentados na tela.
     */
    options: Array<ThfMultiselectOption>;
    /**
     * @optional
     *
     * @description
     *
     * Indica que a lista definida na propriedade t-options será ordenada pelo label antes de ser apresentada no
     * dropdown.
     *
     * @default `false`
     */
    sort: boolean;
    /**
     * @optional
     *
     * @description
     *
     * Indica que o campo iniciará com foco.
     *
     * @default `false`
     */
    focus: boolean;
    /**
     * @optional
     *
     * @description
     *
     * Define o modo de pesquisa utilizado no campo de busca, quando habilitado.
     * Valores definidos no enum: ThfMultiselectFilterMode
     *
     * @default `startsWith`
     */
    filterMode: ThfMultiselectFilterMode;
    /** Pode ser informada uma função que será disparada quando houver alterações no ngModel. */
    change?: EventEmitter<any>;
    protected clickOutListener: () => void;
    protected resizeListener: () => void;
    ngOnInit(): void;
    ngAfterContentChecked(): void;
    validAndSortOptions(): void;
    setUndefinedLabels(options: any): void;
    updateList(options: Array<ThfMultiselectOption>): void;
    updateModelToValidate(): void;
    callOnChange(selectedOptions: Array<ThfMultiselectOption>): void;
    eventChange(selectedOptions: any): void;
    getValuesFromOptions(selectedOptions: Array<ThfMultiselectOption>): (string | number)[];
    getLabelByValue(value: any): string;
    changeItems(selectedValues: any): void;
    searchByLabel(search: string, options: Array<ThfMultiselectOption>, filterMode: ThfMultiselectFilterMode): void;
    compareMethod(search: string, option: ThfMultiselectOption, filterMode: ThfMultiselectFilterMode): boolean;
    startsWith(search: string, option: ThfMultiselectOption): boolean;
    contains(search: string, option: ThfMultiselectOption): boolean;
    endsWith(search: string, option: ThfMultiselectOption): boolean;
    validate(c: AbstractControl): {
        [key: string]: any;
    };
    updateSelectedOptions(values: any): void;
    writeValue(values: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    abstract updateVisibleItems(): void;
}
