import { ElementRef } from '@angular/core';
import { ThfInputGeneric } from '../thf-input-generic/thf-input-generic';
export declare abstract class ThfNumberBaseComponent extends ThfInputGeneric {
    type: string;
    constructor(elementRef: ElementRef);
    eventOnInput(e: any): void;
    validMaxLength(maxlength: number, value: string): string;
    writeValueModel(value: any): void;
    private isEndWithDot;
    private formatNumber;
}
