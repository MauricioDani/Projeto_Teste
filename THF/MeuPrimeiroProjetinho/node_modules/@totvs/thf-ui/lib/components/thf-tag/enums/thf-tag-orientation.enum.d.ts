/**
 * @usedBy ThfTagComponent
 *
 * @description
 *
 * Define os tipos de orientações disponíveis para o `thf-tag`.
 */
export declare enum ThfTagOrientation {
    /** A tag será exibida na horizontal, ao lado direito em relação ao label. */
    Horizontal = "horizontal",
    /** Exibe a tag na vertical, ou seja, abaixo do label. */
    Vertical = "vertical"
}
