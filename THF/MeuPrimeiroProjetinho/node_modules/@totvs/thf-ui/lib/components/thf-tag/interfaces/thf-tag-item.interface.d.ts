/**
 * @docsPrivate
 *
 * @description
 *
 * Interface com a definição do objeto gerado ao clicar no `thf-tag`.
 */
export interface ThfTagItem {
    /** Valor contendo o tipo de tag. */
    type: string;
    /** Valor contendo o texto da tag. */
    value?: string;
}
