import { ThfToolbarAction } from '../thf-toolbar-action.interface';
/**
 * @docsPrivate
 *
 * @usedBy ThfToolbarComponent
 *
 * @description
 *
 * O componente `thf-toolbar-actions` tem como objetivo receber uma lista de ações e um ícone que podem ser personalizados.
 */
export declare class ThfToolbarActionsComponent {
    private _actionsIcon?;
    /** Define uma lista de ações. */
    actions?: Array<ThfToolbarAction>;
    /** Define o ícone das ações. */
    actionsIcon: string;
}
