import { EventEmitter } from '@angular/core';
import { DecimalPipe } from '@angular/common';
import { ThfTableDetail } from './thf-table-detail.interface';
import { ThfTableDetailColumn } from './thf-table-detail-column.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente responsável por apresentar o detalhe de cada linha da tabela.
 */
export declare class ThfTableDetailComponent {
    private decimalPipe;
    private _detail;
    /**
     * Define se a tabela possui a opção de `checkbox` habilitada.
     */
    hasCheckbox?: boolean;
    /**
     * Configuração da linha de detalhes.
     */
    detail: ThfTableDetail;
    /**
     * Lista de itens do _detail_ da tabela.
     */
    items: Array<any>;
    /**
     * Ação executada ao selecionar ou desmarcar a seleção de uma linha de detalhe do `thf-table`.
     */
    selectRow?: EventEmitter<any>;
    constructor(decimalPipe: DecimalPipe);
    readonly detailColumns: Array<ThfTableDetailColumn>;
    readonly typeHeaderInline: boolean;
    readonly typeHeaderTop: boolean;
    formatNumberDetail(value: any, format: string): any;
    getColumnTitleLabel(detail: ThfTableDetailColumn): string;
    onSelectRow(item: any): void;
    private returnThfTableDetailObject;
}
