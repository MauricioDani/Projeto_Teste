import { ThfColorPaletteService } from './../../../services/thf-color-palette/thf-color-palette.service';
import { ThfTableSubtitleColumn } from './../thf-table-subtitle-footer/thf-table-subtitle-column.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente para a criação da representação da legenda, em formato de círculo.
 */
export declare class ThfTableSubtitleCircleComponent {
    private thfColorPaletteService;
    private _subtitle;
    /** Objeto com os dados da legenda. */
    subtitle: ThfTableSubtitleColumn;
    /** Esconde a tooltip. */
    hideTitle: boolean;
    constructor(thfColorPaletteService: ThfColorPaletteService);
}
