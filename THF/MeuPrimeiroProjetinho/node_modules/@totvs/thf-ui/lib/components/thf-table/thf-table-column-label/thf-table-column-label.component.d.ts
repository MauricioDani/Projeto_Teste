import { ThfColorPaletteService } from './../../../services/thf-color-palette/thf-color-palette.service';
import { ThfTableColumnLabel } from './thf-table-column-label.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente para a criação da representação da legenda, em formato de texto .
 */
export declare class ThfTableColumnLabelComponent {
    private thfColorPaletteService;
    private _value;
    /** Objeto com os dados do label */
    value: ThfTableColumnLabel;
    constructor(thfColorPaletteService: ThfColorPaletteService);
}
