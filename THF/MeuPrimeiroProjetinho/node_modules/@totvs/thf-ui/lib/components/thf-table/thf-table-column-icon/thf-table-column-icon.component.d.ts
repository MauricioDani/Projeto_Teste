import { ThfTableColumn } from '../interfaces/thf-table-column.interface';
import { ThfTableColumnIcon } from './thf-table-column-icon.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente responsável por exibir ícones nas colunas.
 */
export declare class ThfTableColumnIconComponent {
    tooltipText: string;
    private _icons;
    column: ThfTableColumn;
    /** Lista de um ou mais ícones que serão exibidos em sua respectiva coluna. */
    icons: Array<ThfTableColumnIcon> | Array<string> | string;
    row: any;
    checkDisabled(iconColumn: ThfTableColumnIcon): any;
    getIconColorClass(columnIcon: ThfTableColumnIcon): string;
    onIconClick(iconColumn: ThfTableColumnIcon): void;
    tooltipMouseEnter(text: string, iconColumn: ThfTableColumnIcon): void;
    tooltipMouseLeave(): void;
    private convertToColumnIcon;
    private getIconColor;
}
