import { ElementRef, EventEmitter } from '@angular/core';
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente que apresenta o campo de pesquisa no thf-menu.
 */
export declare class ThfMenuFilterComponent {
    readonly clean = true;
    loading: boolean;
    inputFilterElement: ElementRef;
    filter: EventEmitter<{}>;
    filterItems(search: string): void;
}
