import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { ThfMenuItemFiltered } from '../thf-menu-item/thf-menu-item-filtered.interface';
import { ThfMenuFilter } from '../thf-menu-filter/thf-menu-filter.interface';
/**
 * @docsPrivate
 *
 * @description
 *
 * Serviço que implementa a interface `ThfMenuFilter`, utilizado para fazer requisições ao serviço informado pelo usuário,
 * caso for uma URL, no componente `thf-menu`.
 */
export declare class ThfMenuService implements ThfMenuFilter {
    private http;
    private _url;
    readonly url: string;
    constructor(http: HttpClient);
    configProperties(url: string): void;
    getFilteredData(search: string, params?: any): Observable<Array<ThfMenuItemFiltered>>;
}
