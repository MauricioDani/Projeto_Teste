/**
 * @usedBy ThfNavbarComponent
 *
 * @description
 *
 * Interface para definição das literais usadas no `thf-navbar`.
 */
export interface ThfNavbarLiterals {
    /** Texto exibido no item de menu no qual os itens do navbar são agrupados quando está no modo responsivo. */
    navbarLinks?: string;
}
