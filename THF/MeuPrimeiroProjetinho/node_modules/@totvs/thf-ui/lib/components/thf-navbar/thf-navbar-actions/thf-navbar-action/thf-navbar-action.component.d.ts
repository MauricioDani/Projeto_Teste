import { ViewContainerRef } from '@angular/core';
import { Router } from '@angular/router';
export declare class ThfNavbarActionComponent {
    private router;
    private param;
    private parentRef;
    action?: Function;
    icon: string;
    label: string;
    link?: string;
    tooltip?: string;
    constructor(viewContainerRef: ViewContainerRef, router: Router);
    onActionClick(): void | Promise<boolean>;
    private openUrl;
}
