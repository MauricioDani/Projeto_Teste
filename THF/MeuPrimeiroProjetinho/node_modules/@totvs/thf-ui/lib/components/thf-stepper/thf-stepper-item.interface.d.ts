import { ThfStepperStatus } from './enums/thf-stepper-status.enum';
/**
 * @usedBy ThfStepperComponent
 *
 * @description
 *
 * Interface para definição dos *steps* do componente `thf-stepper` quando utilizada a propriedade `t-steps`.
 */
export interface ThfStepperItem {
    /** Texto do item do stepper. */
    label: string;
    /** Define o estado de exibição do *step*. */
    status?: ThfStepperStatus;
}
