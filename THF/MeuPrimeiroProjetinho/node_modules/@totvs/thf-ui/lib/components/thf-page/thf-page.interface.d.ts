import { ThfBreadcrumb } from '../thf-breadcrumb/thf-breadcrumb.interface';
/**
 * @usedBy ThfPageComponent
 *
 * @description
 *
 * Interface para o atributo `filter` do componente `thf-page`.
 */
export interface ThfPage {
    /** Objeto com propriedades do breadcrumb. */
    breadcrumb?: ThfBreadcrumb;
    /** Título da página. */
    title?: string;
}
