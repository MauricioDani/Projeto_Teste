import { ThfLanguageService } from '../../../services/thf-language/thf-language.service';
import { ThfLoadingOverlayLiterals } from './interfaces/thf-loading-overlay-literals.interface';
export declare const thfLoadingOverlayLiteralsDefault: {
    en: ThfLoadingOverlayLiterals;
    es: ThfLoadingOverlayLiterals;
    pt: ThfLoadingOverlayLiterals;
};
/**
 *
 * @description
 *
 * Este componente mostra ao usuário uma imagem de _loading_ e bloqueia a página inteira ou o container escolhido,
 * enquanto aguarda a resposta de alguma requisição.
 */
export declare class ThfLoadingOverlayBaseComponent {
    private languageService;
    private _screenLock?;
    private _text?;
    /**
     * @optional
     *
     * @description
     *
     * Define se o _overlay_ será aplicado a um container ou a página inteira.
     *
     * @default `false`
     */
    screenLock: boolean;
    /**
     * @optional
     *
     * @description
     *
     * Texto a ser exibido no componente.
     *
     * > O valor padrão será traduzido acordo com o idioma configurado no [**ThfI18n**](/documentation/thf-i18n) ou navegador.
     *
     * @default `Carregando`
     */
    text: string;
    constructor(languageService: ThfLanguageService);
    private getTextDefault;
}
