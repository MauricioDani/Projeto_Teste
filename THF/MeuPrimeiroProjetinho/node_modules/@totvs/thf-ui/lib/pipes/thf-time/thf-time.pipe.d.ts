import { PipeTransform } from '@angular/core';
/**
 * @docsPrivate
 *
 * @description
 *
 * Pipe responsável por apresentar um horário baseado no formato de entrada definido na API da Totvs.
 */
export declare class ThfTimePipe implements PipeTransform {
    transform(time: string, format?: string): string;
    private addDotMiliseconds;
    private formatValue;
    private getFormatMiliseconds;
}
