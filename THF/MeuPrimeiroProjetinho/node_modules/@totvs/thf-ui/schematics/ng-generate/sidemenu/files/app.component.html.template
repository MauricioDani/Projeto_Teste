<div class="thf-wrapper">
  <thf-toolbar t-title="<%= classify(appName) %>"></thf-toolbar>

  <thf-menu [t-menus]="menus"></thf-menu>

  <thf-page-default t-title="<%= classify(appName) %>">
    <p> <%= classify(appName) %> page </p>
  </thf-page-default>
</div>
