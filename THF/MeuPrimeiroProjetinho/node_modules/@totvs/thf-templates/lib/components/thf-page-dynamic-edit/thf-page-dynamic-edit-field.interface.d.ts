import { ThfDynamicFormField } from '@totvs/thf-ui';
/**
 * @usedBy ThfPageDynamicEditComponent
 *
 * @description
 *
 * Interface dos fields usados para compor o template `thf-page-dynamic-edit`.
 */
export interface ThfPageDynamicEditField extends ThfDynamicFormField {
    /** Indica se o campo será duplicado caso seja executada a ação de duplicação. */
    duplicate?: boolean;
}
