import { OnInit } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { ThfBreadcrumb, ThfPageAction, ThfDialogService, ThfNotificationService } from '@totvs/thf-ui';
import { ThfPageDynamicDetailActions } from './thf-page-dynamic-detail-actions.interface';
import { ThfPageDynamicDetailField } from './thf-page-dynamic-detail-field.interface';
import { ThfPageDynamicService } from './thf-page-dynamic.service';
export declare const thfPageDynamicDetailLiteralsDefault: {
    en: {
        pageActionEdit: string;
        pageActionRemove: string;
        pageActionBack: string;
        confirmRemoveTitle: string;
        confirmRemoveMessage: string;
        removeNotificationSuccess: string;
        registerNotFound: string;
    };
    es: {
        pageActionEdit: string;
        pageActionRemove: string;
        pageActionBack: string;
        confirmRemoveTitle: string;
        confirmRemoveMessage: string;
        removeNotificationSuccess: string;
        registerNotFound: string;
    };
    pt: {
        pageActionEdit: string;
        pageActionRemove: string;
        pageActionBack: string;
        confirmRemoveTitle: string;
        confirmRemoveMessage: string;
        removeNotificationSuccess: string;
        registerNotFound: string;
    };
};
/**
 * @description
 *
 * O `thf-page-dynamic-detail` é uma página que serve para exibir registros em detalhes,
 * o mesmo também suporta metadados conforme especificado na documentação.
 *
 * @example
 *
 * <example name="thf-page-dynamic-detail-basic" title="Totvs Page Dynamic Detail Basic">
 *  <file name="sample-thf-page-dynamic-detail-basic/sample-thf-page-dynamic-detail-basic.component.html"> </file>
 *  <file name="sample-thf-page-dynamic-detail-basic/sample-thf-page-dynamic-detail-basic.component.ts"> </file>
 * </example>
 */
export declare class ThfPageDynamicDetailComponent implements OnInit {
    private router;
    private activatedRoute;
    private thfNotification;
    private thfDialogService;
    private thfPageDynamicService;
    private _actions;
    private _autoRouter;
    private _duplicates;
    private _fields;
    private _keys;
    private _pageActions;
    literals: any;
    model: any;
    /**
     * @optional
     *
     * @description
     *
     * Define as ações da página de acordo com a interface `ThfPageDynamicDetailActions`.
     */
    actions: ThfPageDynamicDetailActions;
    /**
     * @todo Validar rotas na mão pois se existir uma rota '**' o catch do navigation não funciona.
     *
     * @optional
     *
     * @description
     *
     * Cria automaticamente as rotas de edição (novo/duplicate) e detalhes caso as ações
     * estejam definidas nas ações.
     *
     * > Para o correto funcionamento não pode haver nenhum rota coringa (`**`) especificada.
     *
     * @default false
     */
    autoRouter: boolean;
    /** Objeto com propriedades do breadcrumb. */
    breadcrumb?: ThfBreadcrumb;
    /** Lista dos campos exibidos na página. */
    fields: Array<ThfPageDynamicDetailField>;
    /** Título da página. */
    title: string;
    /**
     * @description
     *
     * Endpoint usado pelo template para requisição do recurso que serão exibido.
     *
     * Caso a ação `remove` estiver configurada, será feito uma requisição de exclusão nesse mesmo endpoint passando os campos
     * setados como `key: true`.
     *
     * > `DELETE {end-point}/{keys}`
     *
     * ```
     *  <thf-page-dynamic-detail
     *    [t-actions]="{ remove: '/' }"
     *    [t-fields]="[ { property: 'id', key: true } ]"
     *    t-service="/api/thf-samples/v1/people"
     *    ...>
     *  </thf-page-dynamic-detail>
     * ```
     *
     * Resquisição disparada, onde a propriedade `id` é igual a 2:
     *
     * ```
     *  DELETE /api/thf-samples/v1/people/2 HTTP/1.1
     *  Host: localhost:4000
     *  Connection: keep-alive
     *  Accept: application/json, text/plain
     *  ...
     * ```
     *
     * > Caso esteja usando metadados com o template, será disparado uma requisição na inicialização do template para buscar
     * > os metadados da página passando o tipo do metadado esperado e a versão cacheada pelo browser.
     * >
     * > `GET {end-point}/metadata?type=detail&version={version}`
     */
    serviceApi: string;
    constructor(router: Router, activatedRoute: ActivatedRoute, thfNotification: ThfNotificationService, thfDialogService: ThfDialogService, thfPageDynamicService: ThfPageDynamicService);
    ngOnInit(): void;
    readonly duplicates: any[];
    readonly keys: any[];
    readonly pageActions: ThfPageAction[];
    private confirmRemove;
    private formatUniqueKey;
    private goBack;
    private loadData;
    private loadMetadata;
    private navigateTo;
    private openEdit;
    private remove;
    private resolveUrl;
    private getPageActions;
    private getKeysByFields;
    private getDuplicatesByFields;
    private isObject;
}
