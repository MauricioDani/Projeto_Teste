import { ThfDynamicViewField } from '@totvs/thf-ui';
/**
 * @usedBy ThfPageDynamicDetailComponent
 *
 * @description
 *
 * Interface dos fields usados para compor o template `thf-page-dynamic-detail`.
 */
export interface ThfPageDynamicDetailField extends ThfDynamicViewField {
    /** Indica se o campo será duplicado caso seja executada a ação de duplicação. */
    duplicate?: boolean;
}
