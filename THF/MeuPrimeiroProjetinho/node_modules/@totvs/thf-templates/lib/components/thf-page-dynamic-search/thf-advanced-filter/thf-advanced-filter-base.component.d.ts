import { EventEmitter } from '@angular/core';
import { ThfDynamicFormField } from '@totvs/thf-ui';
export declare const thfAdvancedFiltersLiteralsDefault: {
    en: {
        title: string;
        primaryActionLabel: string;
        secondaryActionLabel: string;
    };
    es: {
        title: string;
        primaryActionLabel: string;
        secondaryActionLabel: string;
    };
    pt: {
        title: string;
        primaryActionLabel: string;
        secondaryActionLabel: string;
    };
};
/**
 * @docsPrivate
 *
 * @description
 *
 * Filtro de busca avançada criado a partir de um formulário dinâmico.
 * Componente de uso interno.
 */
export declare class ThfAdvancedFilterBaseComponent {
    private _filters;
    literals: any;
    /**
     * Coleção de objetos que implementam a interface ThfDynamicFormField, para definição dos campos que serão criados
     * dinamicamente.
     */
    filters: Array<ThfDynamicFormField>;
    /** Função que será disparada e receberá os valores do formulário ao ser clicado no botão buscar. */
    searchEvent: EventEmitter<any>;
}
